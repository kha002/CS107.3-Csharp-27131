using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Welcome to the Kilometer to Meter Converter!");

        Console.Write("Enter a kilometer value: ");
        string input = Console.ReadLine();

        if (double.TryParse(input, out double kilometer))
        {
            ConvertValues converter = new ConvertValues();
            converter.KilometerToMeter(kilometer);
        }
        else
        {
            Console.WriteLine("Invalid input. Please enter a valid number for kilometer value.");
        }

        Console.WriteLine("\nPress any key to exit.");
        Console.ReadKey();
    }
}

class ConvertValues
{
    public void KilometerToMeter(double kilometer)
    {
        double meter = kilometer * 1000;
        Console.WriteLine("Meter value: " + meter);
    }
}
